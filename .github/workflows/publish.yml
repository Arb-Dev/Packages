# Publishing is handled in release-please.yml using release-please action outputs.
# This file is intentionally empty — GitHub requires a valid workflow file,
# so it is kept as a no-op rather than deleted to preserve the badge URL in the README.
name: Publish packages
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing is triggered via release-please.yml"
